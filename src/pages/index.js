import React from "react"
import { Link } from "gatsby"

import { Layout, Hero, Socials, Nav } from "@components";

const IndexPage = ({ location }) => (
	<Layout location={location}>
	</Layout>
)

export default IndexPage
